export interface FilterState {
  id: number;
  filtered: boolean;
}

export interface DifficultyFilterState {
  id: number;
  level: string;
  filtered: boolean;
}

export interface DifficultyState {
  id: number;
  level: string;
}
